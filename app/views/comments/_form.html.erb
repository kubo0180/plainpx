<%= form_for([@px, @comment]) do |f| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :commenter %>
    <%= f.text_field :commenter, :class => 'span5' %>
  </div>
  <div class="field">
    <%= f.label :body %>
    <%= f.text_area :body, :class => 'span5', :style => 'height:4em' %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
